[pytest]
# Run `pytest` from the repo root with no arguments to execute the whole suite.
testpaths = tests
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
    ignore::sklearn.exceptions.ConvergenceWarning
